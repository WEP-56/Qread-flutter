# Qread Flutter 开发基线

更新时间：2026-05-05

## 当前确认状态

- `flutter analyze` 可通过，当前只有 info/warning 级别问题，没有阻塞编译的 error。
- 登录、书源/RSS 源加载链路可用。
- 书架、发现、订阅、我的、搜索、阅读器已有初版实现。
- 发现结果页和书源管理页已补上第一版 Flutter 侧骨架，后续可以直接继续实现行为细节。

## 已确认的开发阻塞项

1. 现有多处 Dart 文件中的中文文案已经出现乱码。
   - 这不是终端显示问题，源码中的字符串本身已经失真。
   - 不影响当前编译，但会影响 UI 质量、排错效率和后续维护。
2. `StorageService` 已支持持久化 `baseUrl/themeMode`，但启动阶段此前没有回灌 `baseUrl`。
   - 本次已补上 `baseUrl` 启动加载。
3. 书源分页接口必须遵循 `Page -> New` 的缓存读取顺序，并准备旧接口 fallback。
   - 搜索页本次已补上正确解析和 fallback。
4. P1 功能里仍有两个关键缺口：
   - 发现分类点击后书籍结果页
   - 书源管理完整能力

## 建议开发顺序

1. 完成发现结果页
   - 补齐分页策略
   - 明确返回结构
   - 接入书籍详情/加入书架后的反馈
2. 完成书源管理页
   - 导入
   - 单条编辑
   - 启停
   - 分组与排序
   - 导出
3. 完成阅读器设置与书签
   - 设置持久化
   - `addbookmark/getbookmark/delbookmark`
4. 完成 RSS 文章列表与详情
   - 分类 tab
   - WebView 渲染
   - 登录态与变量支持

## 本次准备工作的落点

- 新增 `SourceManageProvider`
- 新增发现结果页 `ExploreBooksPage`
- 路由补齐：发现分类 -> 结果页
- 启动时加载已保存的服务端地址
- README 改为项目说明，避免后续接手再从默认 Flutter 模板开始判断
